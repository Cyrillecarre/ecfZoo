<form method="post" action="{{ path('app_recommandation_veterinary_delete', {'id': recommandation_veterinary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recommandation_veterinary.id) }}">
    <button class="btn">Delete</button>
</form>
